import 'package:flutter/material.dart';

class OutageListScreen extends StatelessWidget {
  const OutageListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Outages List')),

      body: const Center(child: Text('Outage List Screen coming soon!')),
    );
  }
}
